const request = require("supertest");
const jokes = require("./jokes-router.js");

describe('jokes-router.js', () => {
  describe('GET /', () => {
    it.todo('should return 200 status when authenticated user request jokes');
  });
});
